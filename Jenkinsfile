pipeline {
    agent {
        label 'ec2'
    }

   
    stages {
        stage('Build') {
            steps {
                
                sh 'terraform --version'

          
            }
        }
    }
}
